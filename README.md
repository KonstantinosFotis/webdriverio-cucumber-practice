# Webdriverio/Cucumber Practice Project

1. On a terminal run ```npm install``` or ```npm install:clean``` to install the dependencies.
2. Then run ```npm test:spec <the name of the feature file you want to run>```